import { Platform } from '@/types';

// Index files for each component should export the tabs, preview, and title
export { tabs } from './tabs';
export { Preview } from './Preview/index';
export const title = 'Icon Choice List';
export const contributors = [{ username: 'maxico_dev', platform: Platform.TWITTER }];
